<%= form_with(model: kudo) do |form| %>
  <% if kudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kudo.errors.count, "error") %> prohibited this kudo from being saved:</h2>

      <ul>
        <% kudo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id, value:current_employee.id, type: :hidden %>
    <%= current_employee.email %>
  </div>

    <div class="field">
    <%= form.label :receiver_id %>
    <%= collection_select( :kudo, :receiver_id, Employee.all, :id, :email, {}, {}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
