<%= form_with(model: [:admin, @company_value ]), url: admin_company_value_index(@company_value) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.submit %>
<% end %>
